{
  "name": "frontend-build-shim",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix ../../frontend && npm run build --prefix ../../frontend && node -e \"const fs=require('fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.mkdirSync('dist',{recursive:true}); fs.cpSync('../../frontend/dist','dist',{recursive:true});\""
  }
}
